# Content source. Leave BOTH unset to build from the local src/content/profile.ts.
# Set them to build this client from the CMS instead:
PAYLOAD_URL=https://cms.yourdomain.se   # your self-hosted Payload origin
CLIENT_SLUG=tangent                     # which client document to build
